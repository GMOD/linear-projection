/**
 * Contains visible regions
 */
import VisibleRegion from "./VisibleRegion";

export default class VisibleRegionContainer{

    sortedArray:Array<VisibleRegion>;

    insertVisibleRegion(region){
    }
}
