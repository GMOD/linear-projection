


exports.projectValue = function (projection, input) {
    console.log('Projecting ' + input + ' with ' + projection);
};
exports.unProjectValue = function (projection, input) {
    console.log('Un projecting ' + input + ' with ' + projection);
};

exports.printMsg = function () {
    console.log("This is a message from the demo package");
};